package controller

import (
	// "fmt"
	// "net/http"

	// "strconv"

	"github.com/gin-gonic/gin"
	// "github.com/fuhiz/docker-go-sample/app/pkg/connecter"
	// "github.com/fuhiz/docker-go-sample/app/pkg/model"
	// "github.com/google/uuid"
)

type BalanceController struct{}

// 変更

func (self *DepositController) CreateBalance(c *gin.Context) {
	// uuidObj, _ := uuid.NewUUID()
	// BalanceID := uuidObj.String()

	// newDeposit := model.NewDeposit(BalanceID, UserID, DepositID, WithdrawalID)
	// deposit, err := model.CreateDeposit(connecter.DB(), newDeposit)

	// if err != nil {
	// 	fmt.Println(err)
	// 	c.JSON(http.StatusBadRequest, gin.H{"error": "deposit create failed"})
	// 	return
	// }

	// c.JSON(http.StatusOK, gin.H{"deposit": deposit})
}
